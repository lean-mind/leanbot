export interface Direct {
  id: string,
  created: any,
  user: string,
  is_archived: boolean,
  is_im: boolean,
  is_org_shared: boolean,
  is_open: boolean,
  has_pins: boolean,
  last_read: any,
  priority: number,
} 